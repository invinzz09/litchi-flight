# Как поставить навык litchi-flight себе

## Что нужно один раз

1. **Claude Code** — десктоп-приложение или CLI, вход под своим аккаунтом Claude.
2. **Браузер на Chromium с расширением «Claude in Chrome»** — подходит Google Chrome или Brave (в Brave проверено, работает), должен подойти и Edge. **Firefox и Safari не подходят** — расширения для них нет. Через расширение Claude управляет браузером (открывает Mission Hub, импортирует маршрут, делает скриншоты).
   - В этом браузере открыть Chrome Web Store (Brave и Edge его тоже открывают), найти **Claude in Chrome** (издатель Anthropic) → **Добавить в Chrome**.
   - Нажать на иконку расширения и войти в **тот же аккаунт Claude**, что и в Claude Code.
   - Расширение ставится только руками — автоматически его установить нельзя. Это делается один раз.
3. **VPN** — Mission Hub (`flylitchi.com/hub`) без него не открывается. VPN должен работать для этого браузера (системный VPN или VPN-расширение в этом же браузере).
4. Скопировать папку `litchi-flight` целиком в
   `C:\Users\<ваше имя>\.claude\skills\litchi-flight\`
   (папку `skills` создать, если её нет; файл `SKILL.md` должен лежать прямо в `litchi-flight\`, а не во вложенной папке из архива). Перезапустить Claude Code — навык появится в списке по `/`.

## Как пользоваться

5. Открыть Claude Code **в папке, где лежат KML участков** (результаты лягут рядом, в `Пролеты\`).
6. Включить VPN.
7. Написать: «сделай пролёт `Участок.kml`» или «сделай пролёт по точке 55°38'54" 95°21'15", высота 100».
8. При первом запуске навык сам проверит, что расширение подключено и хаб открывается, и если чего-то нет — скажет, что именно сделать. Когда хаб попросит войти — войти под аккаунтом Mission Hub организации **в окне браузера, которое открыл Claude**, один раз; дальше сессия сохраняется в профиле браузера. Пароль Claude не спрашивает и не вводит — это его правило безопасности, автоматический вход сделать нельзя.

Навык сам проверит настройки хаба, рельеф, лимиты, сохранит миссию и покажет скриншот — границы нужно посмотреть и, если надо, поправить точки прямо в хабе, Claude подхватит.
